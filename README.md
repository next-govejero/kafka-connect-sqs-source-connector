# kafka-connect-sqs-source-connector
A custom Kafka Connect source connector that streams messages from AWS SQS queues into Apache Kafka topics. Designed for reliable, scalable, and configurable ingestion of SQS messages into Kafka-based data pipelines. Includes support for message batching, visibility timeouts, dead-letter handling, and AWS authentication via IAM roles or credentials
